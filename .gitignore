# ── Dependencies ──
node_modules/

# ── Build outputs ──
client/dist/
client/dist-ssr/

# ── Landing page (separate repo — denchat.top) ──
landing/

# ── Vault (separate repo + origin — vault.denchat.top) ──
vault/

# ── Versioned design docs (kept local, not in the repo) ──
docs/v1/
docs/v2/

# ── Tauri ──
src-tauri/target/
src-tauri/gen/schemas/

# ── Logs ──
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
lerna-debug.log*

# ── Environment / secrets ──
.env
.env.*
!.env.example

# ── OS files ──
.DS_Store
Thumbs.db
desktop.ini

# ── Editor / IDE ──
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.local

# ── TypeScript cache ──
*.tsbuildinfo
