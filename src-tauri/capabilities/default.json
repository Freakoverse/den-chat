{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main DEN Chat window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "core:tray:default",
    "deep-link:default"
  ]
}
